/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    'tailwindcss': {},
    // Opcional pero recomendado: añade autoprefixer
    'autoprefixer': {},
  },
}

export default config
